# Daytona — get an API key at https://app.daytona.io
# If unset, the app falls back to running Python locally via subprocess.
DAYTONA_API_KEY=
DAYTONA_TARGET=us

# Bodhi voice agent — direct WS to bodhiagent.live, no key required for the public agent.
BODHI_WS_URL=wss://bodhiagent.live/ws
